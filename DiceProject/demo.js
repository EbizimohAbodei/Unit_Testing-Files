"use strict";

const temp = new Array(20).fill(2).map((num, index) => num + index);
const temp2 = new Array(20).fill(2).map((num, index) => [num + index]);
const temp3 = new Array(20).fill(2).map((num, index) => num + index);
console.log(temp);
console.log(temp2);
